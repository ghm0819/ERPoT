launch function:

1. build_map.launch
build the OGM for the given /scan_topic via ground segmentation

2. demo.launch
show the ground_segmentation results via patchwork-plus-plus

3. pose_tracking.launch
pose tracking /scan_topic via ground segmentation

4. kitti.launch
provide the kitti topic
